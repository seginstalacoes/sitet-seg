---
const { label = 'FOTO: projeto real da SEG', h = 280 } = Astro.props;
---
<div class="media-ph" style={`min-height:${h}px`} role="img" aria-label={`Espaço reservado para imagem: ${label}`}>
  <span>[{label}]<br/><small>imagem de demonstração — substituir pelo arquivo real</small></span>
</div>
